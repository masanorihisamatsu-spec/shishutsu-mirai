# このファイルをコピーして .env.local を作成してください。
#
#   cp .env.local.example .env.local
#
# Render上の本番APIに接続してフロントエンドをローカルで動かす場合の設定例。
# ローカルのバックエンド（docker compose up 等）に接続する場合は
# http://localhost:8000 に変更してください。
NEXT_PUBLIC_API_URL=https://shishutsu-mirai.onrender.com
